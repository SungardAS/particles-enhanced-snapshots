{

   {{#if cidrLogicalId}}"CidrIp": {"Ref": "{{cidrLogicalId}}"},{{/if}}
   {{#if cidr}}"CidrIp": "{{cidr}}",{{/if}}
   "FromPort": {{fromPort}},
   "GroupId": {{helper "m:core" "propertyValue" securityGroupId}},
   "IpProtocol": "tcp",
   "ToPort": {{toPort}}
}
