{{#resource}}
         "Type": "AWS::IAM::InstanceProfile",
         {{#if retain}}"DeletionPolicy" : "Retain",{{/if}}
	 "Properties": {
            "Path": "/",
    "Roles":{
	"Fn::If" : [
    	"DefinedRole",
    	[{"Ref": "EsAdmin" }],
  	[{"Ref": "{{roleSelectLogicalId}}" }]]}
     }
{{/resource}}
